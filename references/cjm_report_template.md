<!--
  ШАБЛОН cjm_report.md — контракт для сборки финального отчёта режима segment_map.

  ВАЖНОЕ ОТЛИЧИЕ от references/report_template.md (режимы 1-4): там report.py
  (скрипт) рендерит количественные секции программно. Здесь никакого cjm_report.py
  НЕТ — модель, ведущая CJM-прогон, собирает cjm_report.md ВРУЧНУЮ по этому
  шаблону из артефактов стадий 0-5 (включая обязательную Стадию 4.5 —
  таргетинг-матрица, см. SKILL.md §4, spec_synthetic-panel_v1.1_
  segment_map.md §2, spec_synthetic-panel_v1.2.md Модуль 2). Всё, что НЕ в
  HTML-комментариях — статичная структура и
  заголовки, переносятся как есть; `{{ИМЯ}}` — плейсхолдер, подставляется из
  манифеста/артефактов конкретного прогона; парные маркеры вида
  ЧТО_ТО_START/ЧТО_ТО_END —
  границы блока с переменным числом строк (по сегменту/по кандидату) — внутри
  один образец строки, в реальном отчёте таких блоков/строк N.

  Источники подстановки по разделам (v1.3, spec_synthetic-panel_v1.3.md §2.1 —
  добавлены «Главное», «Паспорт методологии», «Что с этим делать», «Приложение»;
  нумерация 1-8 ниже — для контентных разделов, framing-разделы не нумеруются,
  та же логика, что в references/report_template.md):
   - шапка + Главное          <- пишется ПОСЛЕДНИМ шагом (после всех стадий и
                                  сборки количественных разделов) моделью,
                                  ведущей прогон — правила формулировок идентичны
                                  references/report_template.md, раздел «Главное»
                                  (3-5 выводов-решений, без новых цифр, со ссылками
                                  вглубь; см. этот же принцип там)
   - Паспорт методологии      <- studies/cjm_<имя>.yaml + runs/<...>/manifest.json,
                                  человеческим языком (сколько сегментов словами,
                                  метод устойчивости, режим-бейдж стадии 4, если она
                                  пройдена); полная техника (manifest, независимость,
                                  целевой диапазон) — в «Приложение» в конце
   - 1. Легенда карты доверия <- references/disclaimers.md, раздел «Легенда
                                  карты доверия» (копируется ДОСЛОВНО, не пересказывается —
                                  тот же принцип, что и для DISCLAIMER_BLOCK в report_template.md)
   - 2. Фарма-гейт            <- 00_filter.md / 00_filter.yaml (раздел пропускается
                                  целиком, если vertical не начинается на pharma)
   - 3. Сегменты, сводная     <- 01_segments_merged.yaml (core -> основная таблица,
                                  unstable_segments -> таблица-приложение)
   - 4. Карта сегментов и CJM <- 02_cjm_<segment_id>.md (по одному на сегмент ядра)
   - 5. Данные и источники    <- 03_data_merge.md
   - 6. Тест RTB              <- 04_rtb_candidates.yaml (кандидаты, статус 🟡) +
                                  runs/<rtb_study>_<ts>/report.md (результат SSR-теста
                                  через штатный run_study.py/report.py — переносится
                                  ярлык разделимости из раздела 1 того отчёта
                                  («уверенный разрыв/на грани/в пределах шума», v1.3
                                  §1.3 п.3), не E/CI/PMF — те остаются в приложении
                                  ТОГО отчёта, сюда не переносятся вовсе)
   - 7. Проекция на таргетинги<- 05_targeting_<segment_id>.yaml (по одному на сегмент
                                  ядра) + сводный targeting_matrix.yaml, оба через
                                  scripts/targeting_export.py (см. references/
                                  cjm_prompts_ru.md, Стадия 4.5). ОБЯЗАТЕЛЕН, в отличие
                                  от раздела 8 — не пропускать даже в демо/пилоте.
   - 8. Медиазадачи (опц.)    <- 05_media_<segment_id>.md, раздел целиком опускается,
                                  если стадия 5 не запускалась (не оставлять пустым заголовком)
   - Что с этим делать        <- пишется тем же шагом, что и «Главное» — рекомендации
                                  + обязательный пункт «какой human-тест следующий»
                                  (тот же принцип, что references/report_template.md)
   - Границы этого отчёта     <- v1.3: ОДИН раздел с ДВУМЯ подразделами (spec §2.1
                                  п.4 — было три подряд H2-заголовка «Границы этого
                                  отчёта», находка Д6), не «оба блока подряд», как
                                  было в v1.2. Подраздел 1 (общий) — references/
                                  disclaimers.md, DISCLAIMER_BLOCK_START/END (H2 —
                                  этот заголовок и есть заголовок ВСЕГО раздела, см.
                                  ниже секцию 9 шаблона), плейсхолдеры
                                  {{MODEL_ID}}/{{EMBEDDING_MODEL_ID}}/{{RUN_DATE}}/
                                  {{MANIFEST_PATH}} — из manifest.json RTB-под-прогона
                                  стадии 4, НЕ из manifest.json самого CJM-прогона (см.
                                  cjm_init.py, схема manifest не содержит model/
                                  embedding). Подраздел 2 (режимный) — references/
                                  disclaimers.md, DISCLAIMER_BLOCK_CJM_START/END (уже
                                  H3 внутри — вставляется КАК ЕСТЬ, без дополнительного
                                  понижения уровня), {{RUNS_FOR_STABILITY}} — из
                                  manifest.json CJM-прогона. Оба подраздела копируются
                                  ДОСЛОВНО, целиком, без сокращений — правило SKILL.md
                                  §3/§4.
   - Приложение               <- manifest.json CJM-прогона (ссылка) + `runs_for_
                                  stability`/`independence_mode`/`segments_target_range`
                                  дословно + статус линтера (`cjm_lint.py`, exit-код) +
                                  ссылка на manifest.json RTB-под-прогона стадии 4,
                                  если она выполнялась

  Нумерация разделов ниже — ориентир для читаемости, не механическая привязка:
  если раздел 2 (Фарма-гейт) пропущен (vertical не pharma_*), последующие
  разделы можно перенумеровать при сборке или оставить как есть — на
  усмотрение того, кто собирает отчёт (сборка ручная, не скриптовая). Раздел 7
  (Проекция на таргетинги) в этом смысле НЕ похож на раздел 8 (Медиазадачи) —
  он обязателен всегда и никогда не опускается, даже если его тоже
  перенумеровать при сборке.

  Перед сдачей клиенту — ОБЯЗАТЕЛЬНО:
    python scripts/cjm_lint.py --report <путь к собранному cjm_report.md>
  Exit 0 — можно сдавать; exit 1 — список нарушений, прогон не завершён
  (spec §1.2, §4). Легенда и построчная карта доверия ниже — не просто
  оформление, а то, что реально проверяет линтер (см.
  references/cjm_prompts_ru.md, вводный раздел, и сам scripts/cjm_lint.py).
-->

# Отчёт: {{STUDY_NAME}} — Карта сегментов и AI CJM

**{{CATEGORY}} · {{BRAND}}**

> Этот отчёт — вывод синтетической ИИ-панели, режим `segment_map` (кодификация
> AI CJM-цепочки стратегов с исправлением её слабых мест — выдуманные проценты,
> сочинённые цитаты без источника, невоспроизводимость одной чат-сессии,
> искусственно фиксированное число сегментов). Полная карта доверия — раздел 1
> ниже; несъёмные ограничения метода — раздел «Границы этого отчёта» в конце
> документа. Не читать разделы 3-8 в отрыве от них.

---

## Главное

Синтез решающих выводов из разделов разного статуса карты доверия (🟢
модельное качественное / 🟡 гипотеза / 🔴 требует данных — статус конкретного
раздела указан там же, при ссылке на него).

<!--
  Пишется ПОСЛЕДНИМ шагом (после сборки всех разделов 1-8) моделью, ведущей
  прогон — правила формулировок ИДЕНТИЧНЫ references/report_template.md,
  раздел «Главное» (spec_synthetic-panel_v1.3.md §2.1 п.1): 3-5 пунктов,
  «что делать»/«что это меняет» — не пересказ таблиц разделов 3/6/7, без новых
  цифр, каждый пункт со ссылкой на раздел ниже. Для этого режима типичные
  темы «Главного»: какие сегменты ядра устойчивы и на них можно строить
  стратегию (раздел 3), какой RTB-вариант показал уверенный разрыв в тесте
  (раздел 6, если стадия 4 пройдена), что таргетинг-матрица (раздел 7) значит
  для медиазакупки прямо сейчас. Если `vertical: pharma_rx` — первым пунктом
  обязательно упомянуть ограничение ФЗ-38 (раздел 2), не прятать его вглубь.
-->

<!-- KEY_TAKEAWAYS -->

---

## Паспорт методологии 🟢

Фактологическое описание параметров этого прогона (метод, устойчивость,
режим) — модельное качественное 🟢, без домыслов о содержании сегментов.

<!--
  Заменяет прежнюю тех-шапку (список из 9 буллетов) человеческим языком —
  spec_synthetic-panel_v1.3.md §2.1 п.3. Полная техника (manifest, точные
  значения independence_mode/segments_target_range, статус линтера) —
  раздел «Приложение» в конце документа. Пишется тем же шагом, что и стадии
  0-5 (это фактологический пересказ параметров прогона, не интерпретация —
  можно писать сразу после того, как известны {{RUNS_FOR_STABILITY}} и
  {{SEGMENTS_TARGET_RANGE}}, не дожидаясь качественного анализа).
-->

Категория «{{CATEGORY}}», бренд/препарат — {{BRAND}}, вертикаль — {{VERTICAL}}
<!-- pharma_rx | pharma_otc | fmcg | auto | other --> (если начинается на
pharma — фарма-гейт раздела 2 обязателен и учтён). Сегменты аудитории получены
не в одном прогоне, а устойчивостью: {{RUNS_FOR_STABILITY}} независимых
прогон(а/ов) сегментации, режим независимости — {{INDEPENDENCE_MODE_RU}}
<!-- человеческая фраза: "разными субагентами в изолированных контекстах" (full)
     или "последовательно в одном контексте с явной инструкцией не опираться на
     предыдущий результат — допустимо для демо/пилота, не для боевой сдачи" (approximated) -->;
в ядро идут сегменты, воспроизведённые минимум в двух из {{RUNS_FOR_STABILITY}}
прогонов (полное ядро — раздел 3). Целевой диапазон {{SEGMENTS_TARGET_RANGE}}
сегментов — результат этой устойчивости, не заданное заранее число.

{{RTB_STAGE_STATUS_LINE}}
<!--
  Два канонических варианта:
  - стадия 4 (тест RTB) пройдена: «Варианты ключевого сообщения (RTB) для
    приоритетных сегментов проверены той же SSR-панелью, что и обычные клеймы
    — режим прогона теста: {{MODE_BADGE}}. Метод — Semantic Similarity Rating
    (SSR): персона отвечает свободным текстом, число получается через
    сравнение с якорными фразами шкалы, не запрашивается напрямую (полностью
    — `references/methodology.md`, §1, Слой B). Результат — раздел 6.»
  - стадия 4 ещё не пройдена: «Варианты ключевого сообщения (RTB) сформулированы
    как гипотезы (раздел 6.1) — SSR-тест ещё не проводился, режим прогона
    появится после стадии 4.»
-->

Полный технический паспорт (manifest, точные параметры независимости, статус
линтера честности) — раздел «Приложение» в конце документа.

---

## 1. Легенда карты доверия

Три статуса ниже (🟢 модельное качественное / 🟡 гипотеза для проверки / 🔴
требует данных) размечают КАЖДЫЙ раздел этого отчёта — таблица легенды
подставляется целиком из references/disclaimers.md.

<!--
  Легенда — раздел «Легенда карты доверия» в references/disclaimers.md (таблица
  статусов 🟢/🟡/🔴 между заголовком «### Легенда карты доверия» и началом
  маркера DISCLAIMER_BLOCK_CJM_START) — копируется сюда ДОСЛОВНО. Тот
  же принцип, что в references/report_template.md (секция 5): текст определён
  один раз для всех cjm-отчётов и не пересочиняется на каждый прогон.

  Ожидаемое содержание после подстановки — таблица трёх статусов:
    🟢 модельное качественное (мотивации, барьеры, язык, JTBD, стадии CJM)
    🟡 гипотеза для проверки (RTB-кандидаты до теста, медиазадачи, скрытые потребности)
    🔴 требует данных (любая доля/процент/распределение без реального измерения)
  плюс краткое пояснение, что после SSR-теста (раздел 6) RTB-кандидаты получают
  дополнительный статус — измеренный панелью рейтинг, а не 🟡 гипотеза.
-->

---

## 2. Фарма-гейт

<!-- Раздел присутствует ТОЛЬКО если vertical начинается на pharma (pharma_rx
     или pharma_otc); для fmcg/auto/other — не включать этот раздел в отчёт
     вовсе (не оставлять пустым заголовком). -->

- **Источник инструкции:** {{INSTRUCTION_SOURCE_STATUS}} <!-- предоставлена |
  найдена_веб (с URL и датой) | обобщённая_демо -->
- **Статус фильтра:** {{FILTER_TRUST}} (🟢 — инструкция реальна: предоставлена
  или найдена с подтверждённым URL; 🔴 — использована обобщённая демо-инструкция
  категории) <!-- значение — из 00_filter.yaml: instruction_source.trust -->
- **Разрешённая аудитория:** {{ALLOWED_AUDIENCE_SUMMARY}}
- **Исключено из анализа:** {{EXCLUDED_SEGMENTS_SUMMARY}}

<!-- ЕСЛИ vertical == pharma_rx, добавить прямым текстом (принцип §1.6): -->
> **Рецептурный статус.** Коммуникация по данному анализу адресована только
> специалистам здравоохранения (ФЗ-38 ст. 24) — пациентские коммуникационные
> тесты на основе этого отчёта структурно ограничены и требуют отдельного
> юридического согласования до использования вовне.

---

## 3. Сегменты: сводная таблица и устойчивость

Каждый сегмент ядра получен минимум в {{RUNS_FOR_STABILITY}} независимых
прогонах сегментации и несёт метку воспроизводимости — см.
`references/cjm_prompts_ru.md`, стадия 1.2. Оценка размера — ранг, не число
(статус 🔴 в каждой строке ниже); устойчивость и мотивация/барьер — модельное
качественное (🟢).

<!-- CORE_SEGMENTS_TABLE_START -->
<!-- Одна строка на каждый сегмент из 01_segments_merged.yaml: segments[] -->

| Сегмент | Устойчивость 🟢 | Главная мотивация | Главный барьер | Оценка размера 🔴 | Прогоны-источники |
|---|---|---|---|---|---|
| {{SEGMENT_NAME}} (`{{SEGMENT_ID}}`) | {{STABILITY}} | {{MOTIVATION}} | {{BARRIER}} | {{SIZE_ESTIMATE}} | {{SOURCE_RUNS}} |

<!-- CORE_SEGMENTS_TABLE_END -->

### Неустойчивые сегменты (приложение)

<!-- Только если 01_segments_merged.yaml: unstable_segments[] непусто; иначе
     эту подсекцию не включать. Это НЕ ядро — не использовать для RTB-теста
     и клиентских выводов без дополнительного прогона сегментации. -->

<!-- UNSTABLE_SEGMENTS_TABLE_START -->

| Сегмент | Барьер | Комментарий |
|---|---|---|
| {{SEGMENT_NAME}} (`{{SEGMENT_ID}}`) | {{BARRIER}} | воспроизведён только в 1 из {{RUNS_FOR_STABILITY}} прогонов — не использовать без дополнительной проверки |

<!-- UNSTABLE_SEGMENTS_TABLE_END -->

---

## 4. Карта сегментов и CJM 🟢

Полный путь пациента/потребителя по трём этапам — основной содержательный
результат этого отчёта (эквивалент того, что раньше давала цепочка промптов в
чате, но воспроизводимо и с честной маркировкой количественных мест). Перенос
из `02_cjm_<segment_id>.md` — содержательно, не просто ссылкой; длинные списки
формулировок (10-15 пунктов в рабочем файле) можно сократить до наиболее
показательных 5-8, если отчёт становится нечитаемым, но не терять суть и не
терять пометки источника у иллюстративных фраз.

<!-- CJM_SECTION_START -->
<!-- Один блок на каждый сегмент ядра (не на неустойчивые). -->

### Сегмент: {{SEGMENT_NAME}}

**Портрет (кратко):** {{SEGMENT_DESCRIPTION_SHORT}}

#### Этап 1. Осознание проблематики

{{STAGE1_SUMMARY}} <!-- ранние/поздние маркеры, симптомы, диагноз, эмоции,
причины, поведение после осознания — из 02_cjm_<id>.md -->

Соотношение маркер/симптом/диагноз: {{MARKER_SYMPTOM_DIAGNOSIS_STATUS}}
<!-- 🔴 «нет данных — оценка», либо реальные доли с тегом источника из
     03_data_merge.md, если переданы -->

#### Этап 2. Поиск решения

{{STAGE2_SUMMARY}} <!-- стратегии, факторы выбора, источники информации,
барьеры, поисковые запросы, критерии выбора, влияние (ранг, не %), ожидания -->

#### Этап 3. Процесс лечения/использования

{{STAGE3_SUMMARY}}

#### Отличия от других сегментов ядра

<!-- Только если стадия 2.6 (cjm_prompts_ru.md) выполнялась; иначе не включать
     подраздел. -->
{{DIFFERENCES_SUMMARY}}

<!-- CJM_SECTION_END -->

---

## 5. Данные и источники

Статусы: 🟢 — значение подтверждено тегом реального источника; 🔴 — данных нет,
указана оценка, а не измерение. Ни одна цифра с «%» в этом разделе не
сгенерирована моделью «из головы» — см. правило в разделе 1 и в
`references/cjm_prompts_ru.md`, стадия 3.

<!-- DATA_TABLE_START -->
<!-- Один блок таблицы на каждый сегмент ядра, перенос из 03_data_merge.md -->

### Сегмент: {{SEGMENT_NAME}}

| Поле | Статус/значение | Источник | Комментарий |
|---|---|---|---|
| {{FIELD_NAME}} | {{STATUS_OR_VALUE}} | {{SOURCE_TAG}} | {{COMMENT}} |

<!-- DATA_TABLE_END -->

---

## 6. Тест RTB (кандидаты 🟡 → результат SSR-теста)

### 6.1. Кандидаты (до теста)

Каждый кандидат ниже — 🟡 гипотеза для проверки, не факт и не рекомендация, до
того как строка «Результат SSR-теста» ниже заполнена реальным рейтингом.
Формула и правило про недопустимость выдуманной точности доказательства — см.
`references/cjm_prompts_ru.md`, стадия 4.1.

<!-- RTB_CANDIDATES_START -->
<!-- Перенос из 04_rtb_candidates.yaml (форма А или Б, см. cjm_prompts_ru.md §4.3).
     targeted_barrier/targeted_benefit/rtb_type — ОБЯЗАТЕЛЬНЫ в новых прогонах
     (см. cjm_prompts_ru.md §4.2-4.3, находка №3 review_v1.1.md), но
     cjm_to_study.py их не требует принудительно ради обратной совместимости
     со старыми 04_rtb_candidates.yaml, где этих полей нет (например, пилот
     cjm_hairloss_demo_20260710-0017 — там они есть только как комментарий в
     конце файла, не как поля кандидата). ФОЛБЭК: если поле отсутствует у
     конкретного кандидата (в исходном yaml или в notes сгенерированного
     study.yaml) — подставлять «—» в соответствующую ячейку, не оставлять
     плейсхолдер как есть и не пропускать строку/кандидата целиком. -->

| id | Текст | Целевой барьер | Целевой benefit | Тип RTB |
|---|---|---|---|---|
| {{RTB_ID}} | {{RTB_TEXT}} | {{TARGETED_BARRIER}} | {{TARGETED_BENEFIT}} | {{RTB_TYPE}} |

<!-- RTB_CANDIDATES_END -->

### 6.2. Результат SSR-теста

<!--
  v1.3: источник — runs/<rtb_study_name>_<ts>/report.md, собранный ШТАТНЫМ
  scripts/report.py по references/report_template.md — раздел 1 ТОГО шаблона
  (рейтинг с трёхуровневым ярлыком разделимости, spec §1.3 п.3), не E/CI/PMF —
  те остаются в ПРИЛОЖЕНИИ того отчёта и сюда не переносятся вовсе (то же
  правило «относительное важнее абсолютного», доведённое до конца). Переносится
  содержательно (не ссылкой) — тот же ярлык и та же формулировка устойчивости
  между перезапусками, без пересчёта и без правки руками (правило SKILL.md §3:
  посчитанные цифры не трогать).
-->

<!-- RTB_RANKING_TABLE_START -->
<!-- Один блок на каждый протестированный сегмент (test_segments сегментов из
     cjm_<имя>.yaml). Ярлык и пороги P(A>B) — как в references/report_template.md,
     раздел 1 («уверенный разрыв» / «на грани» / «в пределах шума»). -->

#### Сегмент: {{SEGMENT_NAME}}

Шкала: {{SCALE_NAME_RU}} ({{SCALE_ID}})

| Место | RTB-кандидат | Устойчивость разрыва от следующего |
|---:|---|---|
| 1 | {{RTB_ID}} | {{SEPARABILITY_LABEL}} ({{RERUN_STABILITY_PHRASE}}) |
| 2 | … | … |

<!-- RTB_RANKING_TABLE_END -->

> Точность метода — R=0,72 (~90% теоретического потолка) на оригинальной
> англоязычной конфигурации; из результата выше надёжен только относительный
> порядок кандидатов и разрыв между ними, не абсолютное значение E[шкала] как
> таковое (E/CI/PMF полностью — приложение отчёта `runs/<rtb_study_name>_<ts>/
> report.md`; ограничения метода целиком — раздел «Границы этого отчёта» ниже).

---

## 7. Проекция на таргетинги

<!--
  Источник — 05_targeting_<segment_id>.yaml (по одному на каждый сегмент
  ядра) + сводный targeting_matrix.yaml, оба собираются/валидируются
  scripts/targeting_export.py (см. references/cjm_prompts_ru.md, Стадия 4.5).
  Раздел ОБЯЗАТЕЛЕН (в отличие от раздела 8 «Медиазадачи») — не пропускать,
  даже если для части сегментов часть ячеек — «уточнить».
-->

Это правила отбора аудитории для медиазакупки — на кого настраивать показ по
четырём осям, а НЕ прогноз охвата, доли аудитории или размера сегмента на
площадках. Ни одно число в этом разделе не означает количество людей.

**Легенда источников по осям:**

| Ось | Источник | Trust по умолчанию |
|---|---|---|
| Демография (пол/возраст) | поля сегмента (`persona_jitter`) | 🟢 (🔴 на ячейку, где пол — «уточнить») |
| Доход | поля сегмента (`persona_jitter.income_level`) | 🟢 |
| Контент (темы/запросы) | CJM-каналы и поисковые запросы сегмента | 🟡 гипотеза |
| Покупки (аптеки/маркетплейсы) | поведение сегмента в CJM | 🟡 гипотеза |

<!-- TARGETING_TABLE_START -->
<!-- Одна строка на каждый сегмент ЯДРА (не unstable). Значения из
     05_targeting_<segment_id>.yaml — если в ячейке несколько значений,
     перечислять через "; "; полный список с источниками — в самом yaml. -->

| Сегмент | Демография 🟢 | Доход 🟢 | Контент 🟡 | Покупки 🟡 |
|---|---|---|---|---|
| {{SEGMENT_NAME}} (`{{SEGMENT_ID}}`) | {{DEMOGRAPHICS_VALUES}} | {{INCOME_VALUES}} | {{CONTENT_VALUES}} | {{PURCHASES_VALUES}} |

<!-- TARGETING_TABLE_END -->

> **Это правила отбора аудитории, а не прогноз охватов.** Таблица выше не
> говорит и не подразумевает, сколько людей попадёт под эти правила ни на
> одной площадке — это качественный портрет «кого искать», собранный из
> полей сегмента (🟢) и гипотез CJM (🟡), а не измерение медиапланирования.
> Ячейки со значением «уточнить» — честный пробел в данных (обычно пол),
> не оценка. Полные данные с источниками по каждой ячейке —
> `05_targeting_<segment_id>.yaml` и сводный `targeting_matrix.yaml`.

---

## 8. Медиазадачи (опционально) 🟡

<!-- Раздел присутствует ТОЛЬКО если стадия 5 (cjm_prompts_ru.md) запускалась
     для хотя бы одного сегмента; иначе не включать раздел вовсе (не оставлять
     пустым заголовком). Весь раздел — статус 🟡 «гипотеза», включая карту
     тачпоинтов: это творческая интерпретация CJM, не измеренный результат. -->

<!-- MEDIA_SECTION_START -->
<!-- Один блок на каждый сегмент, для которого запускалась стадия 5 -->

### Сегмент: {{SEGMENT_NAME}}

{{MEDIA_CONTENT}} <!-- перенос из 05_media_<segment_id>.md: переходы воронки,
задачи бренда, медиа-решения, карта тачпоинтов, выводы. Конкретные площадки и
формулировки регуляторных ограничений — только с пометкой актуальности
(проверено с URL и датой прогона, либо «по памяти модели, не подтверждено на
дату прогона») -->

<!-- MEDIA_SECTION_END -->

---

## Что с этим делать 🟡

Рекомендации и предложенный следующий шаг — гипотеза к действию 🟡, не
измеренный факт.

<!--
  Пишется тем же шагом, что и «Главное» (после сборки разделов 1-8). Состав —
  тот же принцип, что references/report_template.md, раздел «Что с этим
  делать» (spec §2.1 п.2): рекомендации + ОБЯЗАТЕЛЬНЫЙ пункт «какой human-тест
  следующий». Для этого режима типичный human-тест — количественная волна на
  реальных сегментах для подтверждения размера аудитории (раздел 3 даёт только
  ранг устойчивости, не измерение), или качественные интервью для проверки
  барьеров CJM (раздел 4) на реальных пациентах/покупателях. Если фарма-гейт
  (раздел 2) исключил часть аудитории — здесь же явно напомнить, что
  пациентские коммуникационные тесты на основе отчёта требуют юридического
  согласования до использования вовне.
-->

<!-- NEXT_STEPS -->

---

## Границы этого отчёта

Легенда 🟢/🟡/🔴 из раздела 1 остаётся в силе для всех данных выше — этот
раздел её не заменяет, а дополняет несъёмными ограничениями метода.

<!--
  Абзац выше — ОБЯЗАТЕЛЬНАЯ видимая часть раздела (копировать в отчёт как
  есть): дисклеймер-блоки ниже маскируются линтером, и без видимой строки с
  маркерами заголовок раздела не пройдёт правило 3 cjm_lint.py (закрывает
  вторую часть находки №2 ревью v1.1 на уровне шаблона).

  v1.3 (spec §2.1 п.4, Д6): ОДИН раздел «Границы этого отчёта» с ДВУМЯ
  подразделами вместо трёх подряд одинаково озаглавленных H2, как было в v1.2.
  Заголовок ВСЕГО раздела — H2 «## Границы этого отчёта», который несёт САМ
  общий блок ниже (см. references/disclaimers.md — тот же приём, что и в
  references/report_template.md, где report.py тоже использует этот H2 как
  заголовок секции). Копируются ДОСЛОВНО, ПОДРЯД, оба блока из
  references/disclaimers.md — не сокращать, не пересказывать, не переставлять
  местами:

  1) Блок между маркерами DISCLAIMER_BLOCK_START и DISCLAIMER_BLOCK_END
     (общий для всех отчётов панели — режимы 1-4 и segment_map одинаково; несёт
     H2-заголовок «Границы этого отчёта» — заголовок ВСЕГО этого раздела, не
     повторять его отдельно выше). Плейсхолдеры {{MODEL_ID}}/{{EMBEDDING_MODEL_ID}}/
     {{RUN_DATE}}/{{MANIFEST_PATH}} внутри него — подставляются из manifest.json
     RTB-под-прогона стадии 4 (runs/<rtb_study_name>_<ts>/manifest.json), а НЕ
     из manifest.json самого CJM-прогона (тот не содержит полей модели/
     эмбеддинга — см. cjm_init.py, схема manifest ограничена метаданными
     сегментации). Если стадия 4 в этом прогоне ещё не выполнялась — отчёт не
     должен содержать раздел 6.2 (результат теста) и этот блок временно
     ссылается на "модель уточняется после стадии 4" вместо конкретных значений.

  2) Блок между маркерами DISCLAIMER_BLOCK_CJM_START и DISCLAIMER_BLOCK_CJM_END
     (специфичный для segment_map — устойчивость, честность процентов, цитаты,
     независимость прогонов, фарма-гейт, RTB). Несёт H3-заголовок «###
     Ограничения режима «Карта сегментов» (AI CJM)» — вставляется КАК ЕСТЬ,
     подразделом сразу после блока 1, без дополнительного понижения уровня.
     Плейсхолдер {{RUNS_FOR_STABILITY}} внутри него — из manifest.json
     CJM-прогона (cjm_init.py).

  Оба блока — несъёмная часть каждого cjm_report.md, включая ситуации, когда
  клиент просит «отчёт покороче, без предупреждений» (правило SKILL.md §3).
-->

---

## Приложение 🟢

Технические метаданные прогона — модельное качественное/фактическое 🟢
(manifest, статус линтера, ссылки на под-прогоны), не предмет спора о доверии.

<!--
  Новый раздел v1.3 (spec §2.1 п.3) — полная техника, вынесенная из «Паспорта
  методологии» выше (человеческий язык) и из старой тех-шапки v1.2.
-->

- **Manifest CJM-прогона:** [{{MANIFEST_FILENAME}}]({{MANIFEST_PATH}})
- **Устойчивость сегментации:** {{RUNS_FOR_STABILITY}} независимых прогона(ов),
  режим независимости — {{INDEPENDENCE_MODE}} <!-- full | approximated -->
- **Целевой диапазон сегментов:** {{SEGMENTS_TARGET_RANGE}}
- **Шаблоны промптов:** `references/cjm_prompts_ru.md` (кодифицированная
  переработка цепочки промптов стратегов, см. там же таблицу соответствия)
- **Линтер честности:** {{LINT_STATUS}} <!-- "пройден (exit 0)" перед сдачей клиенту;
  отчёт с exit 1 не считается завершённым (spec §1.2) -->
- **Manifest RTB-под-прогона (стадия 4), если выполнялась:** [{{RTB_MANIFEST_FILENAME}}]({{RTB_MANIFEST_PATH}})
  — модель, версия эмбеддинга, версия якорей, режим (exploratory/validated) и
  полная статистика (E/CI/PMF, P(A>B)) результата раздела 6.2 — там же, в
  приложении ТОГО отчёта (`runs/<rtb_study_name>_<ts>/report.md`, раздел
  «Приложение»).
